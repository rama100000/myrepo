apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmpackage.fullname" . }}
  labels:
    {{- include "helmpackage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmpackage.selectorLabels" . | nindent 4 }}
