apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmpackage.fullname" . }}-test-connection"
  labels:
    {{- include "helmpackage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmpackage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
